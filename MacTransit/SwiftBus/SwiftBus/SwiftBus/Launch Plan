-TransitPrediction conforms to NSCoding
-Replace array of ints with TransitPrediction objects
-Change names of ConnectionHandler and RequestURLs
-Able to call route config from TransitRoute (add agencyTag when downloading routes, not on routeConfig)
-Able to get vehicle locations from TransitRoute object
-Able to get stop predictions from TransitRoute Object (need to store shortName for each stop, predictions can be called on stopName and tag)
-Refresh methods for SwiftBus
-Write tests

Branches are named after Bojack Horseman characters
Ethan - Getting everything ready for launch